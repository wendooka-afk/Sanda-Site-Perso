// shadcn convention: components import `cn` from "@/lib/utils".
// This project's helper already lives in "@/utils/cn" — re-export it so
// both import paths resolve to a single source of truth.
export { cn } from "@/utils/cn";
